package project;

/**
 * Interface for managing user interaction menus in the application.
 * Implementing classes should define how the menu is displayed and handled.
 * This is the interface implemented by the StatsMenuManager class.
 */
public interface MenuManager {
    /**
     * Displays the menu and handles user interaction.
     */
    void showMenu();
}
